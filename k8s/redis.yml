apiVersion: apps/v1
kind: Deployment
metadata:
  name: redisbrige
  labels:
    app: redisbrige
spec:
  selector:
    matchLabels:
      app: redisbrige
  replicas: 1
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: redisbrige
    spec:
      containers:
      - name: master
        image: redis
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 6380
---

apiVersion: v1
kind: Service
metadata:
  name: redisbrige
  labels:
    app: redisbrige
spec:
  ports:
  - port: 6380
    targetPort: 6380
  selector:
    app: redisbrige